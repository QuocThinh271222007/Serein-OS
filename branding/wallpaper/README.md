# branding/wallpaper/

Expected file (see `src/serein/branding/manifest.py`):

- `serein-wallpaper-default-dark.png` (or `.webp`) - the default
  desktop wallpaper: dark navy, subtle mist/rain atmosphere, an
  understated polygon Serein mark, no text-heavy poster or busy
  landscape. Primary target `3840x2160`, crop/scale-safe for
  `2560x1440` and `1920x1080` derivatives. **Not yet provided** -
  pending an asset request; this cannot be derived from the vector
  logo alone (it needs real illustration/atmosphere work).

Nothing in this directory is final branding until that asset request
is fulfilled and the file actually lands here.
